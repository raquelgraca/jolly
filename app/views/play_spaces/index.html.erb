<h1 text-center>Find a play session</h1>
<div d-flex col-6 justify-content-between align-items-center>
<%= simple_form_for :search, url: play_spaces_path, method: "GET" do |f| %>
      <%= f.input :neighbourhood, required: false %>
      <%= f.submit "Search", class: "btn btn-warning" %>
      <%= link_to 'Reset', play_spaces_path %>
  <% end %>
</div>

<div id="playsession-card">
  <div  class="card-columns">
    <%# @play_sessions.each do |play_session|%>
      <div class="card">
        <%# cl_image_tag play_session.photo, :class => "d-block w-100" %>
      </div>
      <div class="card-infos">
        <div>
        </div>
        <%# link_to 'Book Now!', play_session(play_session.id), class: 'btn btn-warning' %>
      </div>
  </div>
  <%# end %>
</div>
