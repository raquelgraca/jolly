
  <div class="container m-8">

  <%= simple_form_for :search, url: appointments_path, method: "GET" do |f| %>
  <div class="bottom d-flex justify-content-between align-items-center">
    <p> Appointments: </p>
        <%= f.submit "Search", class: "btn btn-warning" %>
        <%= link_to 'Reset', appointments_path %>
    <% end %>
  </div>
    <div id="play_spaces">
      <%= render 'form', appointments: @appointments %>
    </div>
    </div>
